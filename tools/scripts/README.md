# tools/scripts

Placeholder para scripts utilitários. Ideias iniciais:

- validar `shared/examples/*.json` contra `shared/schemas/*.schema.json`;
- helper de flash/monitor do firmware;
- gerador de cabeçalhos a partir dos schemas (futuro).
